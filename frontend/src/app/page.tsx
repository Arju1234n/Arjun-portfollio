// src/app/page.tsx
// Next.js App Router home page — imports the client-side portfolio shell.
import PortfolioApp from '@/components/PortfolioApp';

export default function HomePage() {
  return <PortfolioApp />;
}
